updating
forceUpdate
	"Used to bypass the enabled setting and force a modification to the counters, which cause
	 other sessions to refresh their caches."
	

	classChangeCount := classChangeCount + 1.
	sessionMethodChangeCount := sessionMethodChangeCount + 1